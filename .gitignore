# ------------------------------------
# 1. Virtual Environments (สำคัญที่สุด!)
# ------------------------------------
# Python venv (ที่สร้างด้วย python -m venv .venv)
.venv/
# Anaconda/Conda environments
.conda/
env/
venv/ 
# โฟลเดอร์ที่เก็บข้อมูลเฉพาะของแต่ละเครื่อง (ถ้ามีการตั้งชื่อ Environment ต่างไป)
*venv*

# ------------------------------------
# 2. Byte-compiled files (ไฟล์ที่ Python สร้างขึ้น)
# ------------------------------------
__pycache__/
*.py[cod]
*$py.class

# ------------------------------------
# 3. Jupyter Notebook / IPython
# ------------------------------------
# Checkpoints ที่ Jupyter สร้างอัตโนมัติ
.ipynb_checkpoints/
profile_default/
ipython_config.py

# ------------------------------------
# 4. VS Code Files (การตั้งค่าส่วนตัว)
# ------------------------------------
# โฟลเดอร์สำหรับเก็บการตั้งค่า VS Code (ไม่ควร Commit หากเป็นการตั้งค่าส่วนตัว)
.vscode/

# แต่บางครั้งคุณอาจต้องการ Commit ไฟล์เฉพาะ เช่น tasks.json หรือ extensions.json
# หากต้องการรวมไฟล์เฉพาะ ให้เพิ่มเครื่องหมาย ! นำหน้า:
# !.vscode/tasks.json
# !.vscode/extensions.json

# ------------------------------------
# 5. ข้อมูลและผลลัพธ์ (Data and Output)
# ------------------------------------
# ไฟล์ข้อมูลขนาดใหญ่ (เช่น CSV, Excel, DB)
*.csv
*.xlsx
*.sqlite3 
# โฟลเดอร์สำหรับเก็บโมเดลที่ถูกเทรนแล้ว หรือผลลัพธ์
/data/
/output/
/models/

# ------------------------------------
# 6. Build & Packaging (สำหรับโปรเจกต์ที่ซับซ้อน)
# ------------------------------------
build/
dist/
*.egg-info/
